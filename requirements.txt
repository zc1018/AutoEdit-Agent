# Core editing uses Python 3.10+ standard library only.
# Install FFmpeg and FFprobe separately; put both on PATH.
# Optional cloud helpers: pip install -r requirements-api.txt
# Optional Jianying adapter: pip install -r requirements-jianying.txt
